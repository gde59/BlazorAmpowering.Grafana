# BlazorAmpowering.Grafana
A Grafana panel plugin that visualizes **production code coverage** for .NET and Blazor applications.   It displays a collapsible file → class → method tree with hit/miss status, call counts, and coverage percentages — updated live from Prometheus metrics.
